package dev.failures.main.storage;

import dev.failures.main.utils.ChatUtil;

public class MessageValues {
    //PARTY MESSAGES
    public static String LEADER_LEAVE = ChatUtil.colorize("&7Party disbanded due to leader disconnecting.");
    public static String PLAYER_LEAVE = ChatUtil.colorize("&f%name% &7has left the party due to disconnecting.");
}
